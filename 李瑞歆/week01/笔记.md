scikit-learn（sklearn）**提供了上百种模型**，覆盖分类、回归、聚类、降维、异常检测等多个任务。下面按任务类型给你列出最常用的模型（你提到的 **KNeighborsClassifier** 也在其中）：

---

### ✅ **1. 分类模型（Classification）**

| 模型名称 | sklearn类名 |
|----------|-------------|
| K近邻 | `KNeighborsClassifier` |
| 逻辑回归 | `LogisticRegression` |
| 支持向量机 | `SVC` / `LinearSVC` |
| 朴素贝叶斯 | `GaussianNB` / `MultinomialNB` / `BernoulliNB` |
| 决策树 | `DecisionTreeClassifier` |
| 随机森林 | `RandomForestClassifier` |
| 梯度提升树 | `GradientBoostingClassifier` / `HistGradientBoostingClassifier` |
| AdaBoost | `AdaBoostClassifier` |
| 极端随机树 | `ExtraTreesClassifier` |
| 多层感知机（神经网络） | `MLPClassifier` |
| 线性判别分析 | `LinearDiscriminantAnalysis` |
| 二次判别分析 | `QuadraticDiscriminantAnalysis` |

---

### ✅ **2. 回归模型（Regression）**

| 模型名称 | sklearn类名 |
|----------|-------------|
| 线性回归 | `LinearRegression` |
| 岭回归 | `Ridge` |
| Lasso回归 | `Lasso` |
| 弹性网络 | `ElasticNet` |
| 支持向量回归 | `SVR` |
| K近邻回归 | `KNeighborsRegressor` |
| 决策树回归 | `DecisionTreeRegressor` |
| 随机森林回归 | `RandomForestRegressor` |
| 梯度提升回归 | `GradientBoostingRegressor` |
| AdaBoost回归 | `AdaBoostRegressor` |
| 神经网络回归 | `MLPRegressor` |

---

### ✅ **3. 聚类模型（Clustering）**

| 模型名称 | sklearn类名 |
|----------|-------------|
| K均值 | `KMeans` |
| 层次聚类 | `AgglomerativeClustering` |
| DBSCAN | `DBSCAN` |
| 高斯混合模型 | `GaussianMixture` |
| 谱聚类 | `SpectralClustering` |
| 均值漂移 | `MeanShift` |
| OPTICS | `OPTICS` |

---

### ✅ **4. 降维（Dimensionality Reduction）**

| 模型名称 | sklearn类名 |
|----------|-------------|
| 主成分分析 | `PCA` |
| t-SNE | `TSNE` |
| 线性判别分析 | `LinearDiscriminantAnalysis`（也可用于降维） |
| 核PCA | `KernelPCA` |
| 独立成分分析 | `FastICA` |

---

### ✅ **5. 异常检测（Anomaly Detection）**

| 模型名称 | sklearn类名 |
|----------|-------------|
| 孤立森林 | `IsolationForest` |
| 单类SVM | `OneClassSVM` |
| 局部异常因子 | `LocalOutlierFactor` |
| 椭圆包络 | `EllipticEnvelope` |

---

### ✅ 如何快速查看所有模型？

你可以在 Python 中运行：

```python
from sklearn.utils import all_estimators

# 查看所有分类器
classifiers = all_estimators(type_filter='classifier')
print([name for name, _ in classifiers])
```

---

### ✅ 总结一句话：

> sklearn 几乎涵盖了从传统机器学习到现代集成学习、神经网络、聚类、降维等所有主流模型，远不止你熟悉的 `KNeighborsClassifier`。

---

需要我帮你挑一个适合你当前任务的模型？
